<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout</title>
  <link rel="stylesheet" href="/css/user/checkout.css">
  <link rel="stylesheet" href="/css/user/checkout-coupon.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
    /* Modal Overlay */
    .modal-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.6);
      display: none;
      justify-content: center;
      align-items: center;
      z-index: 1000;
      backdrop-filter: blur(2px);
    }
    
    .modal-overlay.show {
      display: flex;
    }
    
    /* Modal Content */
    .modal-content {
      background: white;
      border-radius: 12px;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
      max-width: 500px;
      width: 90%;
      max-height: 90vh;
      overflow-y: auto;
      position: relative;
      animation: modalSlideIn 0.3s ease-out;
    }
    
    @keyframes modalSlideIn {
      from {
        opacity: 0;
        transform: translateY(-20px) scale(0.95);
      }
      to {
        opacity: 1;
        transform: translateY(0) scale(1);
      }
    }
    
    /* Modal Header */
    .modal-content h2 {
      margin: 0;
      padding: 1.5rem 1.5rem 1rem 1.5rem;
      font-size: 1.5rem;
      font-weight: 600;
      color: #2c3e50;
      border-bottom: 1px solid #e9ecef;
      position: relative;
    }
    
    /* Close Button */
    .modal-close {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background: none;
      border: none;
      font-size: 1.5rem;
      color: #6c757d;
      cursor: pointer;
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      transition: all 0.2s ease;
    }
    
    .modal-close:hover {
      background-color: #f8f9fa;
      color: #495057;
    }
    
    /* Form Container */
    .modal-form-container {
      padding: 1.5rem;
    }
    
    /* Form Groups */
    .form-group {
      margin-bottom: 1.25rem;
    }
    
    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: #495057;
      font-size: 0.9rem;
    }
    
    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 0.75rem 1rem;
      border: 2px solid #e9ecef;
      border-radius: 8px;
      font-size: 14px;
      font-family: inherit;
      transition: all 0.2s ease;
      background-color: #fff;
      box-sizing: border-box;
    }
    
    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: #007bff;
      box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
      background-color: #f8f9ff;
    }
    
    .form-group input.error,
    .form-group textarea.error {
      border-color: #dc3545;
      box-shadow: 0 0 0 3px rgba(220, 53, 69, 0.1);
      background-color: #fff5f5;
    }
    
    .form-group input.valid,
    .form-group textarea.valid {
      border-color: #28a745;
      box-shadow: 0 0 0 3px rgba(40, 167, 69, 0.1);
      background-color: #f8fff8;
    }
    
    .error-message {
      color: #dc3545;
      font-size: 12px;
      margin-top: 0.4rem;
      min-height: 16px;
      display: block;
      font-weight: 500;
    }
    
    /* Form Row Layout */
    .form-row {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 1rem;
    }
    
    @media (max-width: 576px) {
      .form-row {
        grid-template-columns: 1fr;
      }
    }
    
    /* Modal Actions */
    .modal-actions {
      display: flex;
      justify-content: flex-end;
      gap: 0.75rem;
      padding: 1.5rem;
      border-top: 1px solid #e9ecef;
      background-color: #f8f9fa;
      border-radius: 0 0 12px 12px;
      margin: 0 -1.5rem -1.5rem -1.5rem;
    }
    
    .modal-actions button {
      padding: 0.75rem 1.5rem;
      border: none;
      border-radius: 6px;
      font-size: 0.9rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.2s ease;
      min-width: 100px;
    }
    
    .modal-actions button[type="button"] {
      background-color: #6c757d;
      color: white;
    }
    
    .modal-actions button[type="button"]:hover {
      background-color: #5a6268;
      transform: translateY(-1px);
    }
    
    .modal-actions button[type="submit"] {
      background-color: #007bff;
      color: white;
    }
    
    .modal-actions button[type="submit"]:hover:not(:disabled) {
      background-color: #0056b3;
      transform: translateY(-1px);
      box-shadow: 0 4px 12px rgba(0, 123, 255, 0.3);
    }
    
    .modal-actions button[type="submit"]:disabled {
      background-color: #6c757d;
      cursor: not-allowed;
      opacity: 0.6;
      transform: none;
    }
    
    /* Required Field Indicator */
    .form-group label .required {
      color: #dc3545;
      margin-left: 2px;
    }
    
    /* Responsive Design */
    @media (max-width: 576px) {
      .modal-content {
        margin: 1rem;
        width: calc(100% - 2rem);
        border-radius: 8px;
      }
      
      .modal-content h2 {
        font-size: 1.25rem;
        padding: 1rem 1rem 0.75rem 1rem;
      }
      
      .modal-form-container {
        padding: 1rem;
      }
      
      .modal-actions {
        padding: 1rem;
        flex-direction: column;
      }
      
      .modal-actions button {
        width: 100%;
      }
    }
  </style>
  <script src="https://checkout.razorpay.com/v1/checkout.js"></script>
</head>
<body>
  <nav class="top-navbar">
    <a href="/" class="navbar-brand">StarForge</a>
    <div class="navbar-user">
      Welcome, <%= user?.name || 'Guest' %>
    </div>
  </nav>

  <div class="checkout-container">
    <h2>Checkout</h2>
    <div class="checkout-grid">
      <div class="shipping-section">
        <div class="section-header">
          <h3>Shipping Address</h3>
          <a href="#" class="btn-primary" data-add-address>Add Address</a>
        </div>

        <div id="addressError" style="display: none; color: red; margin-bottom: 1rem;">
          Please select a shipping address to continue.
        </div>

        <% if (error) { %>
          <div class="error-message" style="display: block; color: red; margin-bottom: 1rem; padding: 10px; background-color: #ffe6e6; border: 1px solid #ff9999; border-radius: 5px;">
            <%= error %>
          </div>
        <% } %>

        <% if (addresses && addresses.length > 0) { %>
          <div class="card-grid">
            <% addresses.forEach(address => { %>
              <div class="info-card <%= selectedAddressId && selectedAddressId === address._id.toString() ? 'selected' : '' %>" data-address-id="<%= address._id %>">
                <div class="card-header">
                  <span class="card-title">
                    <label class="address-selector">
                      <input type="radio" name="selectedAddress" value="<%= address._id %>" 
                             <%= selectedAddressId && selectedAddressId === address._id.toString() ? 'checked' : '' %>
                             class="address-radio" data-address-id="<%= address._id %>">
                      <%= address.name %>
                      <% if (address.isDefault) { %>
                        <span class="default-badge">Default</span>
                      <% } %>
                    </label>
                  </span>
                  <a href="#" class="card-action" data-edit-address="<%= address._id %>">Edit</a>
                </div>
                <p><%= address.address %>, <%= address.city %>, <%= address.district %>, <%= address.state %> - <%= address.pinCode %></p>
              </div>
            <% })  %>
          </div>
        <% } else { %>
          <div class="empty-state">
            <div class="empty-title">No addresses found</div>
            <p class="empty-description">You haven't added any delivery addresses yet.</p>
            <a href="#" class="btn-primary" data-add-address>Add Address</a>
          </div>
        <% } %>

        <div class="section-header">
          <h3>Payment Method</h3>
        </div>
        <select name="paymentMethod" id="paymentMethod" required>
          <option value="" disabled selected>Select Payment Method</option>
          <option value="COD">Cash on Delivery</option>
          <option value="Online">Online Payment</option>
          <option value="Wallet">Pay using Wallet</option>
        </select>
      </div>

      <div class="order-summary">
        <h3>Order Summary</h3>
        <ul>
          <% cartItems.forEach(item => { %>
            <li><%= item.name %> — Qty: <%= item.quantity %> — ₹<%= item.subtotal.toLocaleString('en-IN') %></li>
          <% }) %>
        </ul>

        <div>
          <h3>Coupon</h3>
          
          <!-- Coupon Message Area -->
          <div id="couponMessage" style="display: none; margin-bottom: 10px; padding: 10px; border-radius: 5px;"></div>

          <!-- Applied Coupon Display -->
          <div id="appliedCouponSection" style="<%= coupon ? '' : 'display: none;' %>">
            <p id="appliedCouponText">
              <% if (coupon) { %>
                Applied Coupon: <%= coupon.code %> (<%= coupon.discount %>% off)
              <% } %>
              <button id="removeCouponBtn" class="btn-secondary">Remove</button>
            </p>
          </div>

          <!-- Coupon Input Section -->
          <div id="couponInputSection" style="<%= coupon ? 'display: none;' : '' %>">
            <div class="coupon-box">
              <input type="text" id="couponCode" placeholder="Enter coupon code">
              <button id="applyCouponBtn" class="btn-secondary">Apply</button>
            </div>

            <button id="toggleCouponListBtn" class="btn-secondary" style="margin-top: 10px;">View Available Coupons</button>

            <div id="availableCouponsSection" style="display: none; margin-top: 1rem;">
              <% if (availableCoupons && availableCoupons.length > 0) { %>
                <% availableCoupons.forEach(coupon => { %>
                  <div class="coupon-card">
                    <div>
                      <strong><%= coupon.code %></strong> — <%= coupon.discount %>% off
                      <div style="font-size: 0.85rem; color: #666;">Valid till <%= new Date(coupon.expiryDate).toLocaleDateString('en-IN') %></div>
                      <% if (coupon.minimumAmount && coupon.minimumAmount > 0) { %>
                        <div style="font-size: 0.8rem; color: #e67e22;">Min. order: ₹<%= coupon.minimumAmount.toLocaleString('en-IN') %></div>
                      <% } %>
                    </div>
                    <button class="btn-apply-coupon" data-coupon-code="<%= coupon.code %>">Apply</button>
                  </div>
                <% }) %>
              <% } else { %>
                <p>No active coupons available right now.</p>
              <% } %>
            </div>
          </div>

          <!-- Hidden field to store applied coupon for form submission -->
          <input type="hidden" id="appliedCouponCode" value="<%= coupon ? coupon.code : '' %>">
        </div>

        <table class="sales-table">
          <tr>
            <td>Subtotal</td>
            <td id="subtotalAmount">₹<%= subtotal %></td>
          </tr>
          <tr>
            <td>Discount</td>
            <td id="discountAmount">₹<%= discount %></td>
          </tr>
          <tr>
            <td><strong>Grand Total</strong></td>
            <td id="grandTotalAmount"><strong>₹<%= grandTotal %></strong></td>
          </tr>
        </table>

        <form id="checkoutForm" action="/checkout" method="POST">
          <input type="hidden" id="selectedAddressId" name="addressId" value="<%= selectedAddressId || '' %>">
          <input type="hidden" name="paymentMethod" value="">
          <button type="submit" class="checkout-btn">Place Order</button>
          <p style="margin-top: 5px; font-size: 0.9rem; color: #555;">
            Wallet Balance: ₹<%= user?.walletBalance?.toLocaleString('en-IN') || 0 %>
          </p>
        </form>
      </div>
    </div>
  </div>

  <div class="modal-overlay" id="addressModal">
    <div class="modal-content">
      <div class="modal-header">
        <h2><%= selectedAddressId ? 'Edit Address' : 'Add New Address' %></h2>
        <button type="button" class="modal-close" data-close-modal>&times;</button>
      </div>
      
      <div class="modal-form-container">
        <form id="addressForm" action="" method="POST">
          <!-- Personal Information -->
          <div class="form-row">
            <div class="form-group">
              <label for="name">Full Name <span class="required">*</span></label>
              <input type="text" id="name" name="name" required placeholder="Enter your full name">
              <div class="error-message" id="nameError"></div>
            </div>
            
            <div class="form-group">
              <label for="phone">Phone Number <span class="required">*</span></label>
              <input type="tel" id="phone" name="phone" required placeholder="10-digit mobile number">
              <div class="error-message" id="phoneError"></div>
            </div>
          </div>
          
          <!-- Address Information -->
          <div class="form-group">
            <label for="address">Full Address <span class="required">*</span></label>
            <textarea id="address" name="address" rows="3" required placeholder="House/Flat No., Building Name, Street, Locality"></textarea>
            <div class="error-message" id="addressError"></div>
          </div>
          
          <!-- Location Details -->
          <div class="form-row">
            <div class="form-group">
              <label for="district">District <span class="required">*</span></label>
              <input type="text" id="district" name="district" required placeholder="e.g., Ernakulam">
              <div class="error-message" id="districtError"></div>
            </div>
            
            <div class="form-group">
              <label for="city">City <span class="required">*</span></label>
              <input type="text" id="city" name="city" required placeholder="e.g., Kochi">
              <div class="error-message" id="cityError"></div>
            </div>
          </div>
          
          <div class="form-row">
            <div class="form-group">
              <label for="state">State <span class="required">*</span></label>
              <input type="text" id="state" name="state" required placeholder="e.g., Kerala">
              <div class="error-message" id="stateError"></div>
            </div>
            
            <div class="form-group">
              <label for="pinCode">Pin Code <span class="required">*</span></label>
              <input type="text" id="pinCode" name="pinCode" required placeholder="6-digit PIN code">
              <div class="error-message" id="pinCodeError"></div>
            </div>
          </div>
        </form>
      </div>
      
      <div class="modal-actions">
        <button type="button" data-close-modal>Cancel</button>
        <button type="submit" form="addressForm">Save Address</button>
      </div>
    </div>
  </div>

  <footer>
    <p>© 2025 StarForge. All rights reserved.</p>
  </footer>

  <script>
    window.RAZORPAY_KEY = '<%= razorpayKey %>';
    window.USER_NAME = '<%= user?.name || "Guest" %>';
    window.USER_EMAIL = '<%= user?.email || "" %>';
  </script>
  <script src="/js/user/checkout.js"></script>
</body>
</html>