<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile Example - StarForge</title>
  <link rel="stylesheet" href="/css/user/profileDashboard.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
  <!-- Include the profile navbar -->
  <%- include('../partials/user/profileNavbar', { user, cartCount: cartCount || 0 }) %>

  <!-- Include the updated profile sidebar with proper parameters -->
  <%- include('../partials/user/profileSidebar', { user, currentPage: 'profile' }) %>

  <div class="main-content">
    <div class="content-wrapper">
      <div class="profile-container">
        <div class="page-header">
          <div>
            <h1 class="page-title">Profile Example</h1>
            <p class="breadcrumb">
              <a href="/">Home</a> / <a href="/LoadProfile">Profile</a> / Example
            </p>
          </div>
          <button class="btn btn-primary">
            <i class="fas fa-edit me-2"></i>
            Edit Profile
          </button>
        </div>

        <div class="profile-content">
          <div class="row">
            <div class="col-md-6">
              <div class="info-card">
                <h3>Personal Information</h3>
                <div class="info-item">
                  <label>Full Name:</label>
                  <span><%= user.fullName %></span>
                </div>
                <div class="info-item">
                  <label>Email:</label>
                  <span><%= user.email %></span>
                </div>
                <div class="info-item">
                  <label>Phone:</label>
                  <span><%= user.phone || 'Not provided' %></span>
                </div>
              </div>
            </div>
            <div class="col-md-6">
              <div class="info-card">
                <h3>Account Status</h3>
                <div class="info-item">
                  <label>Status:</label>
                  <span class="badge bg-success">Active</span>
                </div>
                <div class="info-item">
                  <label>Member Since:</label>
                  <span><%= new Date(user.createdAt).toLocaleDateString() %></span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .info-card {
      background: white;
      border-radius: 16px;
      padding: 2rem;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      border: 1px solid #e5e7eb;
      margin-bottom: 2rem;
    }

    .info-card h3 {
      color: #1e293b;
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 1.5rem;
      padding-bottom: 0.75rem;
      border-bottom: 2px solid rgba(252, 161, 32, 0.1);
    }

    .info-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 0.75rem 0;
      border-bottom: 1px solid #f1f5f9;
    }

    .info-item:last-child {
      border-bottom: none;
    }

    .info-item label {
      font-weight: 500;
      color: #64748b;
      margin: 0;
    }

    .info-item span {
      color: #1e293b;
      font-weight: 500;
    }

    .btn-primary {
      background: linear-gradient(135deg, #fca120 0%, #e8940f 100%);
      border: none;
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 12px;
      font-weight: 500;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(252, 161, 32, 0.3);
    }

    .btn-primary:hover {
      background: linear-gradient(135deg, #e8940f 0%, #d17a0a 100%);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(252, 161, 32, 0.4);
    }
  </style>
</body>
</html>
