<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StarForge - Home</title>
    <link rel="stylesheet" href="/css/user/landingPage.css">
    <link rel="stylesheet" href="/css/user/navbar-badges.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>

<body>
  <%- include('../partials/user/navbar', { minimalNavbar: false }) %>
    

        <section class="hero">
            <video autoplay muted loop playsinline class="background-video">
                <source src="/videos/landingPage.mp4" type="video/mp4">
                Your browser does not support the video tag.
            </video>
            <div class="overlay-text">
                <span>SALE</span><br><span>SALE</span><br><span>SALE</span><br><span>SALE</span><br><span>SALE</span>
            </div>
        </section>

        <section class="new-launches" style="background-color: #ffffff; padding: 40px 20px; text-align: center;">
            <h2>New arrivals</h2>
            <div class="product-grid">
                <% if (products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="product-card">
                            <% if (product.mainImage) { %>
                                <img src="<%= product.mainImage %>" alt="<%= product.name %>" style="width: 100%; height: auto;">
                            <% } else { %>
                                <div style="width:100%;height:180px;border:1px dashed #ccc;border-radius:6px;color:#888;display:flex;align-items:center;justify-content:center;">No image</div>
                            <% } %>
                            
                            <h3><%= product.name %></h3>
                            
                            <% if (product.brand) { %>
                                <p class="brand"><%= product.brand %></p>
                            <% } %>
                            
                            <div class="price-container">
                                <% if (product.hasOffer) { %>
                                    <span class="final-price">₹<%= product.finalPrice.toFixed(2) %></span>
                                    <span class="original-price" style="text-decoration: line-through; color: #999; margin-left: 10px;">₹<%= product.price.toFixed(2) %></span>
                                    <span class="offer-badge" style="background: #ff4444; color: white; padding: 2px 6px; border-radius: 3px; font-size: 12px; margin-left: 5px;">
                                        <%= product.offerPercentage %>% OFF
                                    </span>
                                <% } else { %>
                                    <span class="final-price">₹<%= product.finalPrice.toFixed(2) %></span>
                                <% } %>
                            </div>
                            
                            <% if (product.stock > 0) { %>
                                <p class="stock-status" style="color: green; font-size: 12px;">In Stock (<%= product.stock %> available)</p>
                            <% } else { %>
                                <p class="stock-status" style="color: red; font-size: 12px;">Out of Stock</p>
                            <% } %>
                            
                            <% if (product.stock > 0) { %>
                                <a href="/product/<%= product._id %>" class="buy-now">View Details</a>
                            <% } else { %>
                                <button class="buy-now" disabled style="background: #ccc; cursor: not-allowed;">Out of Stock</button>
                            <% } %>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="no-products">
                        <p>No new arrivals available at the moment.</p>
                        <p>Check back soon for exciting new products!</p>
                    </div>
                <% } %>
            </div>
        </section>

        <section class="retro-promo" style="background-image: url('/images/retroCollection.jpg');">
            <div class="overlay-content">
                <h3>Check out our retro collection</h3>
                <p>Discover timeless designs that never go out of style.</p>
                <button class="explore">Explore</button>
            </div>
        </section>

        <section class="promo-header">
            <h2>Newly Launched</h2>
            <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. Scelerisque duis ultrices sollicitudin aliquam
                sem.</p>
        </section>

        <section class="offroad-promo">
            <div class="promo-image" style="background-image: url('/images/offRoadWheels.jpg');"></div>
            <div class="promo-text">
                <h3>Off-Road Wheels</h3>
                <p>
                    Built for adventure, our off-road wheels are designed to withstand the toughest terrains while
                    providing unmatched performance.
                </p>
                <button class="shop-now">View</button>
            </div>
        </section>

        <section class="motorcycle-promo" style="background-image: url('/images/Personalize-comingSoon.jpg');">
            <div class="overlay-content">
                <h3>Coming Soon</h3>
                <p>Experience the thrill with customized wheels.</p>
            </div>
        </section>

        <%- include('../partials/user/footer') %>
            <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
            <script src="/js/user/landingPage.js"></script>
</body>

</html>