<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - StarForge</title>
    <link rel="stylesheet" href="/css/user/forgotPassword.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet">
</head>

<body>
    <div class="background">
        <div class="login-container">
            <h2>Forgot Password?</h2>
            <% if (typeof error !=='undefined' ) { %>
                <p class="error-message">
                    <%= error %>
                </p>
                <% } %>
                    <% if (typeof success !=='undefined' ) { %>
                        <p class="success-message">
                            <%= success %>
                        </p>
                        <% } %>
                            <form action="/forgot-password" method="POST">
                                <div class="input-group">
                                    <label for="email">Registered Email</label>
                                    <div class="input-wrapper">
                                        <input type="email" id="email" name="email" required
                                            aria-describedby="email-icon" />
                                        <span class="icon" id="email-icon"><i class="fas fa-envelope"></i></span>
                                    </div>
                                </div>

                                <button type="submit">Verify Email</button>

                                <div class="divider">Remembered your password? <a href="/login">Sign In</a></div>
                            </form>
        </div>
    </div>

    <script src="/js/user/forgotPassword.js"></script>
</body>

</html>